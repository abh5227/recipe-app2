{
  "name": "seasonal-kitchen",
  "version": "1.0.0",
  "private": true,
  "description": "Client-side scaler/converter tests (Node's built-in test runner, zero deps).",
  "scripts": {
    "test": "node --test tests/js"
  }
}
